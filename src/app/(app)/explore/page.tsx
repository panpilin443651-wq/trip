import { redirect } from "next/navigation";

/**
 * แนะนำเที่ยวย้ายไปเป็นแท็บในหน้าตั้งค่าทริปแล้ว
 *
 * เก็บเส้นทางเดิมไว้เปลี่ยนทางแทนการลบทิ้ง เพราะลิงก์ที่บันทึกไว้ ทางลัด
 * ของแอปที่ติดตั้งบนมือถือ และประวัติเบราว์เซอร์ยังชี้มาที่นี่อยู่
 */
export default function ExploreRedirect() {
  redirect("/settings");
}
